---
description: Notes 笔记的标签规范（frontmatter tags 受控词表）
globs: Notes/**/*.md
alwaysApply: false
---

# Notes 标签规范

`Notes/` 下每篇笔记必须以 frontmatter 开头，标签规则：

- 每篇**恰好 1 个** `类型/` 标签；知识点/练习/实战笔记再加**恰好 1 个** `主题/` 标签；索引文件不打主题标签。
- **禁止**给一篇笔记打多个主题标签（`课程索引.base` 按主题分组依赖此规则，多值列表会破坏 Bases 分组）。
- **禁止**在正文里写行内 `#标签`，标签只出现在 frontmatter。

## 受控词表（新增标签前必须先更新 `Notes/README.md` 的「标签规范」小节和本规则）

- 类型（4 个）：`类型/知识点`、`类型/练习`、`类型/实战`、`类型/索引`
- 主题（11 个）：`主题/语法基础`、`主题/流程控制`、`主题/数组`、`主题/方法`、`主题/类与对象`、`主题/类型系统`、`主题/继承多态`、`主题/泛型`、`主题/集合`、`主题/委托`、`主题/工具链`

## 模板

```yaml
---
tags:
  - 类型/知识点
  - 主题/集合
---
```

新建笔记时同步更新 `Notes/README.md` 的「知识点速查」列表。其余笔记格式要求（单篇 ≤60 行等）见 `Notes/README.md`。
