---
tags:
  - 类型/知识点
  - 主题/继承多态
对应课件:
  - Class02/IHotChair.cs
  - Class02/Lesson07b.cs
aliases:
  - interface
---

# 接口 interface

> [!NOTE]
> 对应 `Class02/IHotChair.cs`、`Lesson07b.cs` 里 `Audi` 的实现
> 前置：[继承、多态、抽象类](./继承与多态.md)

```csharp
// 定义接口：只声明「能做什么」，不写实现
public interface IHotChair
{
    void HotChair();   // 无方法体、无访问修饰符（默认 public）
}

// 类实现接口：基类在前，接口在后，用逗号隔开
class Audi : CarA, IHotChair
{
    // 实现接口方法：必须 public，方法名/签名要一致
    public void HotChair()
    {
        Console.WriteLine($"{nameof(Audi)}有座椅加热");
    }
    // ... 还有从 CarA 继承/重写的 Begin、ChangeGear
}

// 使用：
Audi audi = new Audi();
audi.HotChair();       // 座椅加热（Audi 独有的附加功能）
```

## 要点

- **接口 (interface)** 只规定「必须有哪些方法/属性」（一份契约），**不含实现**。
- 命名习惯：接口名以大写 **`I`** 开头（`IHotChair`）。
- 接口成员**默认 `public`**；实现类里对应方法**必须写 `public`**。
- **类可以实现多个接口**（弥补 C# 只能单继承的限制）：
  - `class Audi : CarA, IHotChair` —— 一个基类 + 一个接口。
  - 语法上**基类必须写在最前面**，接口跟在后面：`: 基类, 接口1, 接口2`。
- 接口的意义：给不同的类**附加统一能力**（如「座椅加热」）。
- 接口也是**多态**：`IHotChair x = new Audi();` 可指向任意实现类。

## 接口 vs 抽象类

| | 接口 interface | 抽象类 abstract class |
|---|---|---|
| 能否有实现 | 传统上不能（只声明） | 可以有普通方法的实现 |
| 一个类能有几个 | **多个**接口 | 只能继承**一个**抽象类 |
| 成员默认可见性 | public | 需显式写 |
| 用途 | 附加「能做什么」的能力 | 表达「是什么」的父子关系 + 共享代码 |

> 小工具：`nameof(Audi)` 返回字符串 `"Audi"`，改类名时会跟着变，比手写字符串安全。
