---
tags:
  - 类型/知识点
  - 主题/类型系统
对应课件: Class02/Lesson06.cs
aliases:
  - value type
  - reference type
  - 栈与堆
---

# 值类型 vs 引用类型（栈与堆）
[下一篇：struct 结构体](./结构体.md)

> [!NOTE]
> 对应 `Class02/Lesson06.cs`
> 前置：[类与对象](./类与对象.md) · 相关：[struct 结构体](./结构体.md)（自定义值类型） · 相关枢纽：[类型系统](../枢纽/枢纽_类型系统.md)

```csharp
// 值类型传参：复制「值」，方法内修改不影响外面
static void Exchange(int a, int b)
{
    int temp = a; a = b; b = temp;
}
int a = 10, b = 20;
Exchange(a, b);
// 结果：a=10, b=20（没变）

// 引用类型传参：复制「地址」，指向同一对象
static void Exchange(ZangAo a, ZangAo b)
{
    int temp = a.Age; a.Age = b.Age; b.Age = temp;
}
ZangAo x = new ZangAo { Age = 5 };
ZangAo y = new ZangAo { Age = 10 };
Exchange(x, y);
// 结果：x.Age=10, y.Age=5（真的交换了）
```

## 内存模型

- **栈 (Stack)**：存局部变量。值类型直接存值；引用类型存的是「指向堆的地址」。
- **堆 (Heap)**：存 `new` 出来的对象，由 GC 管理。

| | 值类型（int、bool、struct） | 引用类型（class、数组、string） |
|---|---|---|
| 变量里存什么 | 值本身 | 对象的地址（引用） |
| 传参复制的 | 值的副本 | 地址的副本 |
| 方法内改动影响外面？ | ❌ 不影响 | ✅ 改「内容」会影响 |

## 关键陷阱

- 改 `a.Age`（顺着地址改堆里的**内容**）→ 外面**能**看到。
- 改 `a` 本身（`a = b`，只换**地址副本**的指向）→ 外面**看不到**。
- Python 对照：不可变对象（int/str）像值传递，list/自定义对象像引用传递，机制一致。
